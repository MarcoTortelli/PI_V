spring:
  application:
    name: ecommerce-api
  datasource:
    url: jdbc:postgresql://localhost:5432/db
    username: admin
    password: 1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration

client:
  card-validator-service:
    url: https://mocki.io/v1/880d7325-43cd-49f3-bb62-151105480e63

admin:
  password: 123456

api:
  security:
    token:
      secret: ${JWT_SECRET:secret-key}

management:
  endpoints:
    web:
      base-path: /
    jmx:
      exposure:
        exclude: health
